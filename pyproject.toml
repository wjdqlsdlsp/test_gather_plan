[tool.poetry]
name = "test-gather-plan"
version = "0.1.0"
description = ""
authors = ["wjdqlsdlsp"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
